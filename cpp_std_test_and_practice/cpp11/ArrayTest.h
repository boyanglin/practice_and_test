#ifndef ARRAY_TEST_H_
#define ARRAY_TEST_H_

#include <string>

namespace General
{
	std::string getFuncName1(unsigned short index);
	unsigned char getFuncDepre1(unsigned short index);

	const char const * getFuncName2(unsigned short index);
	unsigned char getFuncDepre2(unsigned short index);

	std::string getFuncName3(unsigned short index);
	unsigned char getFuncDepre3(unsigned short index);

} //namespace General

#endif // #define ARRAY_TEST_H_